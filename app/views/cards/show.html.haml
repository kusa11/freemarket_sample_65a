= render 'products/top-header'
= render 'users/section/mypage-header'
-# - breadcrumb :user_credit_edit
-# = render 'layouts/breadcrumbs'
.main
  = render 'users/section/mypage-list'
  .card-register-wrapper
    .card-header
      支払い方法
    .card-info
      .card-list
        クレジットカード一覧
      .card-contents
        .card-content
          .card-content__card-type
            = image_tag asset_path('icon/icon_visa.png'), alt: "card", class: "card-icon", width:"49", height:"20"
          .card-content__card-register
            = "************" + @default_card_information.last4
          .card-content__card-dead
            - exp_month = @default_card_information.exp_month.to_s
            - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
            = exp_month + " / " + exp_year
        .card-button
          = form_tag(delete_cards_path, method: :post, id: 'charge-form', name: "inputForm") do
            %input{ type: "hidden", name: "card_id", value: "" }
            %button 削除する
            

= render 'products/top-footer'